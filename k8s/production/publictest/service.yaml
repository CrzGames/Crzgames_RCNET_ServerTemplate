apiVersion: v1
kind: Service
metadata:
  name: prod-seatyrants-worldserver-service-publictest
  namespace: prod-seatyrants-worldserver-publictest
spec:
  clusterIP: None  # Headless service
  selector:
    app: prod-seatyrants-worldserver-publictest
  ports:
    - name: udp
      port: 4001
      targetPort: 4001
      protocol: UDP
